import { defineComponent } from "vue";
import { useConfigStore } from "@/stores";
import { storeToRefs } from "pinia";

export default defineComponent({
  name: "Attributes",
  setup() {
    return () => {
      return <div class="">test - ii</div>;
    };
  }
});
